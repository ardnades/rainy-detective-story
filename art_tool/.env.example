# ============================================================
# art_tool 本機設定範本
# 用法：複製本檔為同目錄下的 .env，再依實際情況填值。
# .env 不會進 repo（已在 .gitignore）。所有欄位皆可留空，留空時走自動偵測。
# ============================================================

# ComfyUI 伺服器 URL。
# 留空 → 自動探測：先試 http://127.0.0.1:8000（Desktop 版），再試 http://127.0.0.1:8188（傳統版）。
# 你目前是 Desktop 版，實際為 8000；若埠號變了再填這裡覆蓋。
# 範例：COMFYUI_URL=http://127.0.0.1:8000
COMFYUI_URL=

# ComfyUI 安裝資料夾（放模型、看 output 用）。
# 留空 → 自動偵測常見位置（M:\ComfyUI、C:\ComfyUI、%USERPROFILE%\ComfyUI 等）。
# 自動偵測失敗時，後台會提示你來這裡填。請填「ComfyUI 主資料夾」絕對路徑。
# 範例：COMFYUI_PATH=M:\ComfyUI
COMFYUI_PATH=
